# Bella's AI 日報 版本規則

建立日期：2026-08-17（台北時間）

## 版本格式

版本號採用語意化版本：

```text
v主版本.功能版本.修正版本
```

範例：

```text
v1.0.0
```

## 大改版：主版本 +1

適用於產品定位、核心架構或主要使用方式改變。

例：

- 前台資訊架構大幅改版（例如這次從「日報列表」改成「Today's Signal ＋判讀全展開」）。
- 資料來源架構大改（RSS 來源、Claude 產文流程重做）。
- 從單頁 news.json 改成年/月/日日期選單、單日獨立網址。
- 新增登入、權限、多使用者。

版本範例：

```text
v0.x → v1.0.0
```

## 中改版：功能版本 +1

適用於新增明確功能，或完成一個使用者可感知的產品能力。

例：

- 新增主題標籤篩選。
- 新增新的內容區塊（例如另開一個趨勢分類）。
- 新增 Header / Footer 版本資訊。
- 新增品質檢查或空日報防呆。
- manifest.json 自動同步機制。

版本範例：

```text
v1.0.0 → v1.1.0
```

## 小改版：修正版本 +1

適用於不改變功能結構的小幅優化、文案修正、樣式微調或 bug fix。

例：

- 調整字級、顏色、間距、底紋。
- 修正文案語氣。
- Now What 拿掉「這週」等時間限定詞。
- 修正來源連結。
- 補某一天日報內容。
- 修 bug（例如切換日期時捲動位置跑掉）。

版本範例：

```text
v1.0.0 → v1.0.1
```

## 目前版本

目前版本：`v1.1.0`

定義：新增「應用切角彙整」六大面向區塊（品牌策略／數位行銷／內容行銷／社群應用／媒體廣告／團隊流程），
判讀提示詞改為資深行銷人視角、更有立場，手機版判讀排版改為主副標同行、內容全寬。

<details>
<summary>歷史版本</summary>

`v1.0.0` — 從「AI 日報」改版為「行銷 × 品牌視角的 AI 趨勢判讀」定位——Today's Signal 資訊卡、
判讀區塊全展開、主題標籤篩選、年/月/日日期選單（單日可分享網址）、新增 footer 版權聲明。

</details>

## 版本升級原則

- 每次更新前先判斷屬於大改版、中改版或小改版。
- Footer 顯示目前網站版本與版本日期，兩者一起更新，不能只改其中一個。
- 每次升版都必須同步更新 `CHANGELOG.md`，記錄修改原因、修改內容與對應 commit。
- 每日自動產出的日報內容（`digests/*.json`、`news.json`）本身不算版本異動，
  只有 `index.html`、`scripts/*.py` 的邏輯或前台呈現改變時才需要升版。
